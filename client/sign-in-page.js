import React from "react";

const SignInPage = () => {
  const onClick = () => {};
  return (
    <React.Fragment>
      <button onClick={onClick}>
        <span>sign in using</span>
      </button>
    </React.Fragment>
  );
};

export default SignInPage;
